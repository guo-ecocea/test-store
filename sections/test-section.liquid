<p>{{ section.settings.title }}</p

{% schema %}
  {
    "name": "Section name",
    "settings": [
      {
        "type": "textarea",
        "id": "title",
        "label": "Title"
      }
    ],
    "presets": [
		{
			"name": "Test section"
		}
	]
  }
{% endschema %}