<p>{{ section.settings.title }}</p

{% schema %}
  {
    "name": "Test section",
    "settings": [
      {
        "type": "textarea",
        "id": "title",
        "label": "Title"
      }
    ],
    "presets": [
		{
			"name": "Test section"
		}
	]
  }
{% endschema %}